# ENVS to create

create file .env

with the 
MAPTILER_API_KEY=yourKey
GRAPHHOPPER_API_KEY=yourGraphHopperKey

# New Rooms/Routes

if you want to add new rooms/routes
please add the rooms/routes via openstreetsmaps and then rerun the buildscripts which create a new json which is used in the app

# Welcome to your Expo app 👋

This is an [Expo](https://expo.dev) project created with [`create-expo-app`](https://www.npmjs.com/package/create-expo-app).

## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npm run android
   ```

